# simple-ts-esm example

## Building
```
yarn install
npx edge-functions@VERSION build --entry main.ts --preset typescript --mode compute
```

## Running in localhost
```
npx edge-functions@VERSION dev
```

